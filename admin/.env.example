# 管理后台前端本地调试用环境变量（Vite 侧）
# 这些变量仅影响本地 `vite dev`，不会打进生产代码
# 生产环境变量在 EdgeOne 项目设置中配置（见 docs/admin-console-plan.md 7.1）

# 本地代理到 edgeone dev 的地址（edgeone makers dev 默认 8088 端口）
VITE_API_BASE=http://localhost:8088

# ---------- 以下为 EdgeOne 项目 firefly-admin 运行时 env（勿把真实 webhook 提交进仓库）----------
# PUBLIC_ALLOWED_ORIGIN=https://astro.cchaoka.cn
# WECHAT_WORK_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=...
# FRIEND_APPLY_WEBHOOK=   # 可选，覆盖友链申请专用机器人；未设则用 WECHAT_WORK_WEBHOOK
